<Window x:Class="Tuan_7.Views.KhaoSatKH"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Tuan_7.ViewModels"
        xmlns:views="clr-namespace:Tuan_7.Views"
        mc:Ignorable="d">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:ThongTinKHViewModel}">
            <views:UcThongTinKH/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:KhaoSatMucDoViewModel}">
            <views:UcKhaoSatMucDo/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:DanhSachPhanHoiViewModel}">
            <views:UcDanhSachPhanHoi/>
        </DataTemplate>


        <!-- Sidebar Button -->
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="52"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1D4ED8"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Top Button -->
        <Style x:Key="MainActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Width" Value="170"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1D4ED8"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Card -->
        <Style x:Key="InfoCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="22"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="24"/>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0"
                Background="White"
                BorderBrush="#E2E8F0"
                BorderThickness="0,0,1,0">
            <StackPanel Margin="22,35,22,20">

                <TextBlock Text="KHẢO SÁT&#x0a;KHÁCH HÀNG"
                           FontSize="22"
                           FontWeight="Bold"
                           Foreground="#1E293B"
                           TextAlignment="Center"
                           Margin="0,0,0,35"/>

                <Button Content="Thông tin khách hàng"
                        Style="{StaticResource SidebarButtonStyle}"
                        Command="{Binding OpenThongTinKHCommand}"/>

                <Button Content="Khảo sát / Góp ý"
                        Style="{StaticResource SidebarButtonStyle}"
                        Command="{Binding OpenKhaoSatCommand}"/>

                <Button Content="Danh sách phản hồi"
                        Style="{StaticResource SidebarButtonStyle}"
                        Command="{Binding OpenDanhSachPhanHoiCommand}"/>
            </StackPanel>
        </Border>

        <!-- Main -->
        <Grid Grid.Column="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="80"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top bar -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding TieuDeTrang}"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="#0F172A"/>
                    <TextBlock Text="{Binding MoTaTrang}"
                               FontSize="13"
                               Foreground="#64748B"
                               Margin="0,6,0,0"/>
                </StackPanel>

                <Button Grid.Column="1"
                        Content="+ Phản hồi mới"
                        Style="{StaticResource MainActionButtonStyle}"
                        VerticalAlignment="Center"
                        Command="{Binding ThemPhanHoiMoiCommand}"/>
            </Grid>

            <!-- Content -->
            <Border Grid.Row="1"
                    Style="{StaticResource InfoCardStyle}">
                <ContentControl Content="{Binding CurrentView}"/>
            </Border>
        </Grid>
    </Grid>
</Window>