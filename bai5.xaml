<UserControl x:Class="Tuan_10.Views.bai5"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Width="820"
             Height="487"
             Background="White">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Quản lý Môn học"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,15"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="VerticalAlignment" Value="Center"/>
                    <Setter Property="Margin" Value="10,5"/>
                </Style>

                <Style TargetType="TextBox">
                    <Setter Property="Height" Value="22"/>
                    <Setter Property="Margin" Value="0,5,10,5"/>
                </Style>

                <Style TargetType="ComboBox">
                    <Setter Property="Height" Value="22"/>
                    <Setter Property="Margin" Value="0,5,10,5"/>
                </Style>
            </Grid.Resources>

            <TextBlock Text="Mã môn:" Grid.Row="0" Grid.Column="0"/>
            <TextBox Grid.Row="0" Grid.Column="1"
                     Text="{Binding SelectedMonHoc.MaMonHoc, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Tên môn học:" Grid.Row="0" Grid.Column="2"/>
            <TextBox Grid.Row="0" Grid.Column="3"
                     Text="{Binding SelectedMonHoc.TenMonHoc, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Số TC:" Grid.Row="1" Grid.Column="0"/>
            <TextBox Grid.Row="1" Grid.Column="1"
                     Text="{Binding SelectedMonHoc.SoTC, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Tính chất:" Grid.Row="1" Grid.Column="2"/>
            <ComboBox Grid.Row="1" Grid.Column="3"
                      SelectedValue="{Binding SelectedMonHoc.TinhChat, Mode=TwoWay}"
                      SelectedValuePath="Content">
                <ComboBoxItem Content="Tự chọn"/>
                <ComboBoxItem Content="Bắt buộc"/>
            </ComboBox>
        </Grid>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,10">
            <Button Content="Thêm" Width="80" Margin="5" Command="{Binding AddCommand}"/>
            <Button Content="Sửa" Width="80" Margin="5" Command="{Binding EditCommand}"/>
            <Button Content="Xóa" Width="80" Margin="5" Command="{Binding DeleteCommand}"/>
            <Button Content="Lưu" Width="80" Margin="5" Command="{Binding SaveCommand}"/>
            <Button Content="Hủy" Width="80" Margin="5" Command="{Binding CancelCommand}"/>
        </StackPanel>

        <DataGrid Grid.Row="3"
                  Margin="0,10,0,0"
                  AutoGenerateColumns="False"
                  ItemsSource="{Binding ListMonHoc}"
                  SelectedItem="{Binding SelectedMonHoc, Mode=TwoWay}"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã môn" Binding="{Binding MaMonHoc}" Width="*"/>
                <DataGridTextColumn Header="Tên môn học" Binding="{Binding TenMonHoc}" Width="2*"/>
                <DataGridTextColumn Header="Số TC" Binding="{Binding SoTC}" Width="0.5*"/>
                <DataGridTextColumn Header="Tính chất" Binding="{Binding TinhChat}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>